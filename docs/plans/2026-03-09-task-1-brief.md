# Task 1 — Remove preset gate and establish new contracts

Read this brief first. It is the exact task scope.

Repository: D:\code\ai\dsh-shell-parser\dsh-shell-selector

Implement only the first plan task. Work directly in the existing repository. Preserve unrelated behavior.

Requirements:

1. Remove all product semantics that gate Shell Selector by Agent Preset:
   - no readBootGate
   - no isGated
   - no gated or gated-by-preset reason/state
   - no defaultAgentPreset or AGENT_PRESETS_SETTINGS_NAMESPACE dependency
   - no default Agent Preset rewriting
2. Change resolveEffective(config, platform, availability) and restartRequired to have no gate parameter. Resolver decisions must depend only on configuration, OS, and detected availability.
3. Preserve mode default/fallback/explicit, strict Windows fallback Bash -> pwsh -> Windows PowerShell, active/configured restart semantics, and platform defaults.
4. Update host/web/client wire types and state builders to remove gate fields and gate warning data. Add source/executable metadata only if needed to support the subsequent detector task; do not redesign the client here.
5. Remove old preset dependency from index/runtime bootstrap without deleting preset files in this task.
6. Update existing resolver/settings/web/runtime tests so they assert the new contract. Add tests for Bash/pwsh independence if appropriate, but leave deeper scoped-agent tests for Task 3.
7. Search the changed source/tests for the forbidden legacy names before finishing.

Constraints:
- Do not use preset names to decide shell capability.
- Do not modify DSH itself.
- Do not use child_process.exec.
- Keep changes restart-bound.

Before editing, inspect current files. Use TDD: update/add failing tests, implement, run the covering tests and typecheck. Do not make broad UI or detector changes in this task.

Report: write a concise completion report to docs/plans/2026-03-09-task-1-report.md with status, files, tests and concerns. Commit the implementation with a focused message.
