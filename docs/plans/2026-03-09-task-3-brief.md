# Task 3 — Agent-scoped official shell tool adaptation

Read this brief first. It is the exact task scope.

Repository: D:\code\ai\dsh-shell-parser\dsh-shell-selector

Implement after Tasks 1 and 2 are complete.

Requirements:

1. Use current official DSH APIs, not preset-name branching:
   - agent/created synchronous event before first prompt
   - agent.ctx.tools.get/register/restrict
   - scopeOf if needed
   - agent.ctx.systemPrompt.section same-name scoped shadow
2. Add required DSH peer/dev dependencies for the agent, scope, tools, system-prompt, dsh-tool-bash, and dsh-tool-pwsh APIs used by the plugin and patch. Keep versions compatible with the current rc.6 install and update pnpm lockfile through the package manager.
3. Patch in only the active official host tool source alongside the unique active executor. A Bash-active process gets host dsh-tool-bash; a PowerShell-active process gets host dsh-tool-pwsh.
4. On each Agent creation inspect final scope visibility of standard tool names bash and pwsh. Shell capability is true iff at least one is visible. Do not inspect or compare standard/code/shell-selector ids.
5. If capability is absent, keep the Agent shell-free: restrict inherited bash and pwsh and shadow tool:bash and tool:pwsh prompt sections with empty text.
6. If capability is present, register/shadow only activeShell's official host definition in the Agent's own scope, restrict the inactive tool in inherited layers, and shadow the inactive dialect prompt section. The visible model tool and prompt must match the active executor: Git Bash/Bash -> bash; PowerShell 7/Windows PowerShell -> pwsh.
7. Keep official DSH execution path. No child_process.exec, no custom shell process bridge, no hot-swap ctx.shell.
8. Add focused tests using fake tool/systemPrompt/agent contexts proving:
   - shell-enabled composition with only pwsh follows active bash;
   - shell-enabled composition with only bash follows active pwsh;
   - custom shell-enabled composition follows active shell without id checks;
   - no-shell composition exposes neither bash nor pwsh;
   - inactive dialect prompt is suppressed.

Before editing inspect current DSH declarations in node_modules and host plugin lifecycle. Use TDD. Run new tests and typecheck. Write docs/plans/2026-03-09-task-3-report.md with files/tests/concerns and commit focused changes.
