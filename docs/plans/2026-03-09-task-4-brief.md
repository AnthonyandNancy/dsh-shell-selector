# Task 4 — DSH-native Settings selector UI

Read this brief first. It is the exact task scope.

Repository: D:\code\ai\dsh-shell-parser\dsh-shell-selector

Implement after host state/detector contracts are available.

Requirements:

1. Remove every native select and option from Shell Selector client code. Do not add a UI framework.
2. Build a reusable Menu-backed selector using the public @deepseek-ai/dsh-client-ui-primitives exports:
   - Menu for floating anchored popover and option rows
   - Button for trigger/actions
   - IconCheckOutline16 for selected check
   - IconChevronDownOutline14 for trigger affordance
3. Use DSH --dsw-* tokens for all colors, typography, border, radius, selected/hover, focus, and shadow styling. Do not hard-code white/black or remove focus without replacement.
4. Support closed trigger, open menu, selected option, disabled option, click selection, Escape, outside click, focus, and ArrowUp/ArrowDown/Home/End keyboard navigation. Preserve aria menu/menuitem semantics.
5. Use compact content-sized triggers (roughly 150–240px rather than full-width 420px). Use Settings-row layout: label/description on the left and control on the right on desktop, stacked on narrow widths.
6. Mode options: DSH default, Auto fallback (recommended), Specific shell. Only explicit/specific mode shows the second Shell selector.
7. Shell options display readable name and muted one-line executable/version detail with ellipsis. Use Git Bash label for Git Bash detections.
8. Keep Save/Re-detect semantics, restart hint, active/configured rows, read-only state, and localized zh-CN/en-US copy. Remove every preset gate warning and field.
9. Add client component/helper tests that do not depend on browser-native select behavior. If no DOM test dependency exists, add a minimal test dependency or isolate and test interaction state helpers plus a component smoke test.

Before editing inspect current page/client CSS and DSH Menu/Button/icon types. Use TDD. Run client typecheck and client tests. Write docs/plans/2026-03-09-task-4-report.md with files/tests/concerns and commit focused changes.
